/.bundle/
/.yardoc
/_yardoc/
/coverage/
/doc/
/pkg/
/spec/reports/
/tmp/

# rspec failure tracking
.rspec_status

# собранный гем: make build кладёт его в корень репозитория
*.gem

# результаты оценочных задач: артефакт прогона, а не исходник. Каждый запуск
# переписывает файл целиком, и в истории от него остались бы замеры чужой
# модели на чужой машине, выглядящие как замеры этой.
/evals/results/
